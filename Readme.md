# Node API
Pre-required package
```sh
npm install -g prettier
```

Install package when first run
```sh
npm install
```

Start server local
```sh
npm run start
```

Build project
```sh
npm run build
```